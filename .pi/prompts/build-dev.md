---
description: Build the project in dev mode and copy the binary to the skill folder
---

Build the Rust project in dev profile and copy the resulting binary into the skill folder.

Steps:
1. Run `cargo build` (dev profile) in the project root.
2. Copy the binary from `target/debug/linear-skill` to `skill/linear-skill`.
3. Report the file size and confirm success.
